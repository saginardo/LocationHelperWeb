<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
<!--
    <properties resource="db.properties">

    </properties>
-->


    <settings>
        <setting name="cacheEnabled" value="true"/>
    </settings>

    <typeAliases>
<!--        &lt;!&ndash; 单个别名定义 &ndash;&gt;
        <typeAlias alias="device" tycom.notinglife.webapppps.LocationHelper.domain.LocationDevice"/>-->
        <!-- 批量别名定义，扫描整个包下的类，别名为类名（首字母大写或小写都可以） -->
        <package name="com.notinglife.webapp.LocationHelper.domain"/>
    </typeAliases>

    <!--Spring 接管数据库和sqlSessionFactory-->
<!--    <environments default="development">
        <environment id="development">
            &lt;!&ndash; 使用jdbc事务管理&ndash;&gt;
            <transactionManager type="JDBC" />
            &lt;!&ndash; 数据库连接池&ndash;&gt;
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driver}" />
                <property name="url" value="${jdbc.url}" />
                <property name="username" value="${jdbc.username}" />
                <property name="password" value="${jdbc.password}" />
            </dataSource>
        </environment>
    </environments>-->

<!--
    <mappers>
&lt;!&ndash;        <mapper resource="device.xml"/>
        <mapper resource="com/notinglwebappapps/LocationHelper/mapper/LocationDeviceMapper.xml"/>&ndash;&gt;
        <mapper resource="device.xml"/>
        &lt;!&ndash;注册指定包下的所有mapper接口,需要mapper的xml文件和接口文件同名&ndash;&gt;
        <package nacom.notinglife.webapppps.LocationHelper.mapper"></package>
    </mappers>-->



</configuration>